<h1>All Books</h1>


<% @books.each do |book| %>
   <h3><%= link_to book.title, book_path(book.id) %></h3>
   <p><%= book.author %></p>
   <p><%= book.description %></p>
   <p><%= book.votes %></p>
<% end %>

<%= link_to "Add a Book", new_book_path %>
